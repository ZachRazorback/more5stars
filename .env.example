# Copy to .env and fill in. NEVER commit .env.
PORT=3000
NODE_ENV=development

# Admin dashboard login (seeded on first boot)
ADMIN_EMAIL=admin@more5stars.net
ADMIN_PASSWORD=changeme123

# Optional: where the SQLite file lives
# DATA_DIR=./data
